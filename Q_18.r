# Generate and store a sequence of values from 10 to -20 in steps of 0.5. Reverse this sequence efficiently without recreating it manually.
seq_values <- seq(10, -20, by = -0.5)
rev_seq_values <- rev(seq_values)  
